<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>💌 One question...</title>
  <style>
    body{
      margin:0; height:100vh; display:grid; place-items:center;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial;
      background: radial-gradient(circle at top, #ffe6f1, #fff);
      color:#1b1b1b;
    }
    .card{
      width:min(520px, 92vw);
      background:#ffffffcc; backdrop-filter: blur(8px);
      border:1px solid #ffd1e6;
      border-radius:24px; padding:28px;
      box-shadow: 0 16px 40px rgba(0,0,0,.08);
      text-align:center;
    }
    h1{ margin:0 0 10px; font-size: 28px;}
    p{ margin:0 0 22px; font-size: 16px; opacity:.9;}
    .btns{ display:flex; gap:12px; justify-content:center; flex-wrap:wrap;}
    button{
      border:0; padding:12px 18px; border-radius:999px;
      font-size:16px; cursor:pointer;
      box-shadow: 0 10px 18px rgba(0,0,0,.08);
      transition: transform .12s ease;
    }
    button:active{ transform: scale(.98); }
    .yes{ background:#ff4d88; color:white; }
    .no{ background:#f2f2f2; }
    .footer{ margin-top:18px; font-size:12px; opacity:.7;}
    .hidden{ display:none; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Hey 💖</h1>
    <p>I made you a tiny website for one important question…</p>

    <h2 style="margin:0 0 18px;">Will you be my Valentine?</h2>

    <div class="btns" id="btns">
      <button class="yes" id="yesBtn">Yes 💘</button>
      <button class="no" id="noBtn">No 🙈</button>
    </div>

    <div id="result" class="hidden">
      <h2 style="margin:10px 0 8px;">YAY!!! 🥰</h2>
      <p id="msg">Okay… now I owe you a proper date 💐</p>
      <p class="footer">Screenshot this and send it to me 😄</p>
    </div>

    <p class="footer">Made with ❤️ on GitHub Pages</p>
  </div>

  <script>
    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");
    const btns = document.getElementById("btns");
    const result = document.getElementById("result");

    // Make "No" run away a bit 😄
    noBtn.addEventListener("mouseenter", () => {
      const x = (Math.random() * 200) - 100;
      const y = (Math.random() * 160) - 80;
      noBtn.style.transform = `translate(${x}px, ${y}px)`;
    });

    yesBtn.addEventListener("click", () => {
      btns.classList.add("hidden");
      result.classList.remove("hidden");
    });
  </script>
</body>
</html>
